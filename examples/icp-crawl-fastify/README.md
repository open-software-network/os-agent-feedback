# Async crawl API ICP

A Firecrawl-style async job API. Job creation and in-progress polls are left
untouched. The completed job response receives feedback instructions and is
grouped by the application's own agent run ID.
