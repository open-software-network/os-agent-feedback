# Review — chunk 6 (CI/CD) — grok

## Blocking
- annotated-tag SHAs used as `uses:` pins — `ci.yml:28,112` (and every other `pnpm/action-setup` / `dorny/paths-filter` pin in `ci.yml`) — `pnpm/action-setup@b0f76dfb…` and `dorny/paths-filter@6852f92c…` are **git tag objects**, not commits (`gh api …/git/tags/<sha>` peels to commits `0ebf471…` / `d1c1ffe…`). Actions only resolves commit SHAs (or tag *names*). These pins fail at runtime with unable-to-resolve-action before any make target runs — CI is dead on arrival. Fix: pin peeled commits `pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9` and `dorny/paths-filter@d1c1ffe0248fe513906c8e24db8ea791d46f8590 # v3.0.3` (or equivalent release commits). Other pins (`checkout`/`setup-node`/`cache`/docker/`dtolnay/rust-toolchain`) verified as real commits. [ran]

## Non-blocking
- no always-green aggregator job — `ci.yml` — path-filtered jobs `if: outputs.X == 'true'` skip when filters miss. Skipped required checks ≠ success under branch protection (merge hangs) or, if not required, a README-only PR gets a green `changes` job and nothing else. Add a final `ci-ok` job `if: always()` that fails on any needed failure and succeeds when all applicable jobs succeeded/skipped. [read]
- `docs-contract` not a direct docs-job step — old `docs.yml` ran `node --test tests/docs-contract.test.mjs` explicitly; `ci.yml` docs job only `make docs-validate` + `docs-a11y`. Covered today because every `docs:` filter path is also under `node:` (incl. `protocol/**`, `sdk/**`, `backend/public/app.js`, `tests/docs-contract.test.mjs`) so `make node-test` always co-runs. Fragile if filters diverge later. [read]
- `node:` filter correctly includes `backend/**` — `ci.yml:42` — matches coupling: `tests/setup-page.test.mjs` / `team-page` / `observability-ui` read `backend/src/{main,store,models}.rs` and `backend/public/*` (regression class of `581c37f`). [read]
- production API never auto-deploys — `promote.yml` is `workflow_dispatch` only; `build-api.yml` only `staging` + SHA tags; no `workflow_run` chain. Landing `deploy-landing.yml` auto-deploys **marketing** `epode` to production on `landing-page/**` main push — matches master brief, not API. [read]
- staging pins exact SHA — `build-api.yml:86` `IMAGE_REF: …/os-epode-api:${{ needs.build.outputs.sha_short }}`; mutable `:staging` is push-only label, never the Railway pin. `cancel-in-progress: false` on build/promote/landing; CI cancels only on PR. [read]
- promote rollback path — `from-tag` staging|hex SHA → inspect digest + `org.opencontainers.image.revision` → require `IMAGE:sha_short` same digest → deploy that SHA ref; re-tag `:production` after success. Fails closed if labels/tags missing. Rollback = dispatch with older 7-char SHA tag still on GHCR. [read]
- permissions — `ci.yml` contents:read only; `build-api` packages:write for push, deploy-staging job overrides contents:read; `promote` packages:write for imagetools; `deploy-landing` contents:read. No local `./.github/actions`. [read]
- make targets — every `run: make …` name exists in Makefile (`backend-fmt-check|clippy|test`, `node-install`, `biome-check`, `node-test`, `landing-check`, `sdk-node-test`, `docs-validate`, `docs-a11y`). Node **22** everywhere; `pnpm/action-setup` before `setup-node` + `cache: pnpm`. [read]
- landing serve stack — `Caddyfile` `:{$PORT:8080}` matches Railway `PORT`; `healthcheckPath: /` hits `index.html`; `/app` `/app/` → 308 to auth (asserted in tests); unscoped `header X-Content-Type-Options nosniff` is site-wide (not stuck on `@static_assets`); Cache-Control only on css/svg/png — HTML uncached-by-header (heuristic browser cache possible). Missing CSP/frame-ancestors/referrer for a static marketing page — acceptable nit. `deploy-landing` paths `landing-page/**` cover Caddyfile/Dockerfile/railway.json. `.dockerignore` + `rm -f /srv/Caddyfile` keep config out of docroot (test-locked). [read + orchestrator e2e]
- scope — only `.github/workflows/*`, `landing-page/{Caddyfile,Dockerfile,.dockerignore,railway.json}`, `tests/rendered-html.test.mjs`; `docs.yml` deleted. No evidence of live Railway mutation in the tree. [ran git status]
- `dtolnay/rust-toolchain@4cda84d…` with `toolchain: "1.95"` matches `backend/Cargo.toml` `rust-version = "1.95"`. [ran]

## Verdict
FIX-FIRST — replace annotated-tag object SHAs for `pnpm/action-setup` and `dorny/paths-filter` with peeled commit SHAs or CI never starts; deploy safety otherwise looks sound.
